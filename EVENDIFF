#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
   while(t--)
   {
   	int n,od=0,ev=0,a;
   	cin>>n;
   	for(int i=0;i<n;i++)
   	{
   		cin>>a;
   		if(a%2!=0)
   		od+=1;
   		else
   		ev+=1;
	}
	if(od>ev)
	cout<<ev<<endl;
	else
	cout<<od<<endl;
   }
}
