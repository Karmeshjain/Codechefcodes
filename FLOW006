#include <stdio.h>

int main(void) 
{
	
	   int t;
    scanf("%d",&t);
    while(t--)
    {
    	int num,sum=0,rem;
    	scanf("%d",&num);
    	while(num!=0)
    	{
    	    rem=num%10;
    		sum=sum+rem;
    		num=num/10;
		}
		printf("%d\n",sum);
    	
	}
	return 0;
}

