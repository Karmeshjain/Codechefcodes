
#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(void) 
{
	
	 int n;
	 scanf("%d",&n);
	 vector<int> a;
	 for(int i=0;i<n;i++)
	 {
	 	int t;
	 	scanf("%d",&t);
	 	a.push_back(t);
	 }
     sort(a.begin(),a.end());
	 for(int i=0;i<n;i++)
	 {
	 	printf("%d\n",a[i]);
	 }
	return 0;
}

