#include <iostream>
#include<math.h>
using namespace std;
int main(void) 
{
	
	int t;
    scanf("%d",&t);
    while(t--)
    {
      int num;
      scanf("%d",&num);
      int pr=sqrt(num);
	  printf("%d\n",pr);

	}
	return 0;
}

